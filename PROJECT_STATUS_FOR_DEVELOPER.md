# 🕌 مشروع زيزو وبلال - ملف حالة المشروع للمطور

> **ابعت الملف ده لأي مطور قبل ما يبدأ شغل.**
> آخر تحديث: فبراير 2026

---

## ⚡ الفكرة في 30 ثانية

```
شيخ بيقول درس ديني
  ↓
AI (Google Gemini) بيحلله ويقسمه لأقسام (آيات، أحاديث، فوائد، قصص، شخصيات...)
  ↓
الأدمن بيراجع ويعدل من لوحة التحكم
  ↓
المحتوى بيظهر على الموقع العام في شكل كروت جميلة
```

---

## 🛠️ التكنولوجيا المستخدمة

| المكون | التقنية | الملاحظات |
|--------|---------|-----------|
| Backend | **Node.js + Express.js** | `server.js` = السيرفر الرئيسي |
| Database | **MongoDB Atlas** (Mongoose) | الاتصال من `.env` |
| Frontend | **HTML + CSS + Vanilla JS** | مفيش React/Vue - كله Vanilla |
| AI | **Google Gemini API** | مخطط - لسه مش متكامل في الكود |
| Auth | **bcryptjs + express-session + connect-mongo** | نظام تسجيل دخول شغال |
| Icons | **Font Awesome** (CDN) | مستخدم في الأقسام الهرمية |

---

## 📁 هيكل المشروع الكامل

```
zizo-bilal/
│
├── 🖥️ server.js                    # السيرفر الرئيسي (Express + MongoDB + Auth)
├── .env                             # متغيرات البيئة (MONGODB_URI, SESSION_SECRET)
│
├── 📄 الصفحات
│   ├── website.html                 # 🌐 الموقع العام (80KB - 1355+ سطر) ⛔ ممنوع اللمس
│   ├── admin_panel_v4_merged.html   # 📊 لوحة الأدمن الرئيسية (65KB) ← الأكتر استخدام
│   ├── admin_v4.html                # 🔐 صفحة Login
│   ├── index.html                   # 📊 لوحة أدمن قديمة (MongoDB - مرجع)
│   ├── admin.html                   # 📊 لوحة أدمن قديمة (localStorage - مرجع)
│   └── admin_panel_v4.html          # 📊 نسخة أدمن سابقة
│
├── 🎨 التصميم
│   ├── style.css                    # التصميم الرئيسي (25KB)
│   └── v3_enhancements.css          # تحسينات CSS إضافية
│
├── 📦 Models (MongoDB Schemas)
│   ├── Lesson.js                    # ✅ نموذج الدرس (Schema ضخم - 34+ قسم)
│   ├── Admin.js                     # ✅ نموذج الأدمن (username + hashed password)
│   ├── Section.js                   # ✅ نموذج الأقسام (Section Registry - 15 قسم)
│   ├── Sheikh.js                    # ✅ نموذج الشيوخ
│   └── Category.js                  # ✅ 🆕 نموذج الأقسام الهرمية (شجري - parent/child)
│
├── 🛣️ Routes (API Endpoints)
│   ├── lessons.js                   # CRUD دروس + rawSource مخفي
│   ├── admin.js                     # Auth (login/logout/check) + Admin CRUD
│   ├── sections.js                  # CRUD أقسام
│   ├── sheikhs.js                   # CRUD شيوخ
│   └── categories.js               # ✅ 🆕 CRUD أقسام هرمية + tree + children
│
├── 📦 scripts/
│   └── create_admin.js              # سكريبت إنشاء أدمن جديد
│
├── 🚀 ملفات التشغيل
│   ├── start_server.py              # تشغيل السيرفر (Python → node server.js)
│   ├── Start_Server.bat             # تشغيل السيرفر (Windows batch)
│   ├── upload.py                    # رفع بيانات لـ MongoDB
│   └── upload.bat                   # رفع بيانات (Windows batch)
│
├── 📋 التوثيق
│   ├── README.md                    # ⭐ سجل المشروع الشامل (900+ سطر!)
│   ├── VIBE_CODING_GUIDE.md         # 📖 دستور العمل (7 خطوات ذهبية)
│   ├── STATUS.md                    # حالة سريعة
│   └── PROJECT_STATUS_FOR_DEVELOPER.md  # 📄 الملف ده!
│
└── 📦 package.json                  # Dependencies
```

---

## 🔗 الروابط والـ Endpoints

### صفحات
| الصفحة | الرابط | الوصف |
|--------|--------|-------|
| الموقع العام | `http://localhost:3000/website` | عرض الدروس للزوار |
| لوحة الأدمن | `http://localhost:3000/admin` | إدارة كاملة (محمية بـ login) |
| صفحة Login | `http://localhost:3000/login` | تسجيل دخول الأدمن |

### API Endpoints
| Endpoint | Method | الوصف |
|----------|--------|-------|
| `/api/lessons` | GET | جلب كل الدروس (بدون rawSource) |
| `/api/lessons/:id` | GET/PUT/DELETE | عمليات على درس واحد |
| `/api/sections` | GET/POST/PUT/DELETE | إدارة الأقسام (Section Registry) |
| `/api/sheikhs` | GET/POST/PUT/DELETE | إدارة الشيوخ |
| `/api/categories` | GET/POST | 🆕 الأقسام الهرمية (root) |
| `/api/categories/all` | GET | 🆕 كل الأقسام (للأدمن) |
| `/api/categories/children/:parentId` | GET | 🆕 أبناء قسم معين |
| `/api/categories/tree` | GET | 🆕 شجرة الأقسام كاملة (nested) |
| `/api/categories/:id` | PUT/DELETE | 🆕 تعديل/حذف قسم + أبنائه |
| `/api/admin/login` | POST | تسجيل دخول |
| `/api/admin/logout` | POST | تسجيل خروج |
| `/api/admin/check` | GET | التحقق من الجلسة |
| `/health` | GET | Health Check |

---

## ✅ المميزات الشغالة (ما تم بناؤه)

### 1️⃣ الموقع العام (`website.html`) ✅
- عرض الدروس بكروت جميلة (Dark mode + ذهبي)
- ترجمة عربية كاملة (165+ مفتاح) - مفيش كلمة إنجليزي
- Navigation ديناميكي (sticky + RTL + scroll centering)
- أقسام ديناميكية تقرأ أي JSON من الـ AI
- Fade-in animations (IntersectionObserver)
- Responsive (موبايل + ديسكتوب)

### 2️⃣ لوحة الأدمن (`admin_panel_v4_merged.html`) ✅
- **نظام Auth**: Login/Logout + Session management
- **إدارة الدروس**: إضافة درس جديد + AI Input + Import Modal
- **إدارة الأقسام**: عرض/إضافة/تعديل/حذف (Section Registry)
- **إدارة الشيوخ**: عرض/إضافة/تعديل/حذف
- **معاينة**: Preview لأي درس
- **Import Modal**: استيراد JSON أو نص خارجي + rawSource بنفسجي
- **إحصائيات**: عدد الدروس + الأقسام + الشيوخ

### 3️⃣ 🆕 نظام الأقسام الهرمية (Categories) ✅
- **تاب جديد "الأقسام الرئيسية"** في لوحة الأدمن
- **بنية شجرية لا نهائية**: قسم ← قسم فرعي ← قسم فرعي أعمق...
- **Breadcrumb Navigation**: 🏠 الرئيسية ← القسم ← القسم الفرعي
- **Icon Picker**: شريط أيقونات سريعة (12 أيقونة شائعة) + حقل يدوي
- **Color Picker**: لون مخصص لكل قسم
- **كروت تفاعلية**: hover effects + icons + وصف
- **CRUD كامل**: إضافة + تعديل + حذف (يحذف الأبناء تلقائياً)
- **API مستقل**: `routes/categories.js` مع tree endpoint

### 4️⃣ Backend ✅
- **Express Server** مع middleware كامل
- **MongoDB Atlas** اتصال مع Mongoose
- **Session-based Auth** مع connect-mongo
- **CORS + JSON parsing** مكوّن
- **rawSource** مخفي من الـ API العام (`.select('-rawSource')`)

---

## ❌ اللي لسه متعملش (مخطط)

### الأولوية 🔴 (القادم)
| # | الميزة | الوصف |
|---|--------|-------|
| 1 | **AI Integration (Gemini)** | ربط الـ AI فعلياً - مش مجرد input يدوي |
| 2 | **Lesson Editor Per-Section** | تعديل كل قسم في الدرس لوحده |
| 3 | **Dynamic Prompt Builder** | بناء البرومبت من الأقسام الموجودة في الـ DB |
| 4 | **ربط Categories بالدروس** | كل درس ينتمي لقسم هرمي |
| 5 | **Website يعرض Categories** | الموقع العام يعرض الأقسام الهرمية |

### الأولوية 🟡 (لاحقاً)
| # | الميزة |
|---|--------|
| 6 | Auto-Save (localStorage) |
| 7 | Toast Notifications |
| 8 | Drag & Drop Reordering |
| 9 | Search & Filter |
| 10 | Export/Import JSON |

### الأولوية 🟢 (مستقبلي)
| # | الميزة |
|---|--------|
| 11 | RAG System + Chatbot |
| 12 | Mobile App (React Native) |
| 13 | Multi-language |
| 14 | Community Features |
| 15 | PDF/Word Export |

---

## 🔒 قواعد الحماية (مهم جداً!)

> **⛔ اقرأ ده قبل ما تلمس أي حاجة:**

| القاعدة | التفاصيل |
|---------|----------|
| ⛔ ممنوع مسح أي ملف | `write_to_file` مع `Overwrite: true` = ممنوع |
| ⛔ ممنوع لمس `website.html` | الموقع العام (80KB) - CSS بس مسموح |
| ⛔ ممنوع لمس `admin.html` | نسخة احتياطية Legacy |
| 💾 Git Commit قبل أي تعديل | `git add -A && git commit -m "قبل تعديل [الوصف]"` |
| 🎯 CSS فقط للتصميم | تغيير الشكل = CSS variables أو inline styles |
| 📖 اقرأ `VIBE_CODING_GUIDE.md` | الدستور الكامل للشغل |

---

## 🏗️ المعمارية (Architecture)

```
┌─────────────────────────────────────────────┐
│                   Frontend                   │
│                                              │
│  website.html ──── الموقع العام (للزوار)     │
│  admin_panel_v4_merged.html ─── لوحة الأدمن  │
│  admin_v4.html ──── صفحة Login               │
│                                              │
└───────────────────┬─────────────────────────┘
                    │ HTTP (fetch)
┌───────────────────┴─────────────────────────┐
│              server.js (Express)             │
│                                              │
│  Middleware: cors, json, session, static     │
│                                              │
│  Routes:                                     │
│  ├── /api/lessons    → routes/lessons.js     │
│  ├── /api/sections   → routes/sections.js    │
│  ├── /api/sheikhs    → routes/sheikhs.js     │
│  ├── /api/categories → routes/categories.js  │
│  └── /api/admin      → routes/admin.js       │
│                                              │
└───────────────────┬─────────────────────────┘
                    │ Mongoose
┌───────────────────┴─────────────────────────┐
│             MongoDB Atlas                    │
│                                              │
│  Collections:                                │
│  ├── lessons     (Lesson.js)                 │
│  ├── admins      (Admin.js)                  │
│  ├── sections    (Section.js)                │
│  ├── sheikhs     (Sheikh.js)                 │
│  └── categories  (Category.js) 🆕            │
│                                              │
└─────────────────────────────────────────────┘
```

---

## 📊 Lesson Schema (المهم جداً)

الدرس في MongoDB بيبقى كده:

```javascript
{
  title: "عنوان الدرس",
  subtitle: "عنوان فرعي",
  rawSource: "🔒 النص الأصلي للشيخ (مخفي من API)",

  overview: { message: "...", points: ["..."] },
  podcast: [{ title: "...", content: "..." }],
  characters: [{ name: "...", role: "...", events: ["..."] }],

  quranHadith: {
    ayat: [{ text: "...", surah: "...", meaning: "..." }],
    hadith: [{ text: "...", purpose: "..." }]
  },

  fiqh: { rewards: [...], sunan: [...], warnings: [...] },
  questions: { trueFalse: [{ question, answer, isCorrect }] },
  benefits: [{ text: "...", category: "..." }],
  stories: [{ title: "...", stages: [...] }],

  // + أي أقسام ديناميكية تانية من الـ AI...
}
```

---

## 🆕 Category Schema (الجديد)

```javascript
{
  name: "اسم القسم",              // مطلوب
  icon: "fa-solid fa-folder",      // Font Awesome class
  color: "#DAA520",                // لون مخصص
  parentId: ObjectId | null,       // null = قسم رئيسي، ObjectId = فرعي
  order: 0,                        // ترتيب العرض
  description: "وصف القسم",
  isActive: true
}
```

**الفكرة**: أقسام هرمية بلا نهاية:
```
📖 القرآن الكريم
  ├── 📜 التفسير
  │   ├── تفسير ابن كثير
  │   └── تفسير الشعراوي
  ├── 🎵 التجويد
  └── 📝 الحفظ

📿 السنة النبوية
  ├── الأحاديث
  └── السيرة
```

---

## 🚀 كيف تشغّل المشروع

```bash
# 1. التثبيت
npm install

# 2. التشغيل
python start_server.py
# أو
node server.js
# أو
npm run dev

# 3. إنشاء أدمن (أول مرة)
node create_admin.js

# 4. الاختبار
# الموقع: http://localhost:3000/website
# الأدمن: http://localhost:3000/admin
```

---

## 💡 ملاحظات للمطور الجديد

1. **الملف الأهم**: `README.md` → فيه كل تاريخ المشروع (900+ سطر)
2. **قبل أي تعديل**: اقرأ `VIBE_CODING_GUIDE.md` (7 خطوات ذهبية)
3. **التصميم**: Dark mode + ألوان ذهبية = الهوية البصرية
4. **اللغة**: الواجهة كلها **عربي 100%** (RTL)
5. **الـ AI**: الـ schema مصمم يستقبل أي JSON من Gemini - ديناميكي 100%
6. **rawSource**: حقل سري يحفظ النص الأصلي - **ممنوع يظهر** في الموقع العام
7. **الملفات القديمة** (`admin.html`, `index.html`): مراجع فقط - متتعدلش

---

## 🎯 لو عايز تساعد - ابدأ من هنا

### مهام سهلة (Good First Issues):
1. إضافة Toast Notifications (بدل `alert()`)
2. تحسين responsive design للموبايل
3. إضافة Dark/Light mode toggle

### مهام متوسطة:
4. ربط الأقسام الهرمية (Categories) بالموقع العام
5. Lesson Editor per-section
6. Auto-save في localStorage

### مهام متقدمة:
7. Gemini API Integration
8. RAG System للشات بوت
9. Dynamic Prompt Builder

---

> **📖 المراجع الكاملة:**
> - `README.md` - السجل الشامل + 105 اقتراح + دروس مستفادة
> - `VIBE_CODING_GUIDE.md` - الدستور والقواعد
> - `baseline_audit.md` - تحليل المميزات feature-by-feature
